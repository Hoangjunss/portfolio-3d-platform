# Every value below is required. Generate secrets with: openssl rand -base64 32
DB_PASSWORD=
JWT_ACCESS_SECRET=
ANALYTICS_IP_HASH_SECRET=
NEXT_PUBLIC_API_BASE_URL=https://api.portfolio.com
IMAGE_TAG=latest

# Optional. Lead notification mail is best-effort: unset means notifications are dropped
# silently while the leads themselves still save.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
