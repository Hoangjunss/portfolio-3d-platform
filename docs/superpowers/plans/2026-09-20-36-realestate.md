# Real Estate Demo Site Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build the `realestate` demo template site (`templates/realestate/`) — the sixth of the 29
site plans under the interactive-demo templates program. Run a real per-site Hallmark design pass,
scaffold the static Next.js export composing `Hero` → `ItemGrid`(listings, with a static
map-placeholder graphic per card) → `InquiryForm` → `Footer` from `@portfolio/template-kit`, and
build its "Save listing" (favorite) interactive feature on `useLocalCollection` +
`SavedItemsPanel`.

**Architecture:** This plan is plan 36 in the interactive-demo spec's §9 execution order — one of
plans 31-59, each independent once plan 30 lands. It has one hard prerequisite:
`2026-09-20-30-template-kit-scaffold.md` must be complete and committed (`@portfolio/template-kit`
built, tested, exporting `Hero`, `ItemGrid`, `InquiryForm`, `Footer`, `SavedItemsPanel`,
`useLocalCollection`, `TemplateTheme`/`assertValidTheme` from its barrel `src/index.ts`). This plan
does not modify `template-kit` itself — it only consumes it. Task 1 runs a real Hallmark design
session (not a scaffold/placeholder step) and commits its own theme output; Task 2 scaffolds the
Next.js app and static composition; Task 3 builds the save-listing interactive feature, its
interaction test, and the design-scoring self-audit.

**Tech Stack:** Next.js 15 / React 19 static export (`output: 'export'`), TypeScript, Vitest +
Testing Library + jsdom for the interaction test, `@portfolio/template-kit` as a local workspace
dependency. No backend change — this plan touches nothing under `backend/`.

**Spec:** `docs/superpowers/specs/2026-09-20-site-realestate-design.md` (section composition, seed
data shape/content, save-listing data flow, Hallmark design brief) plus the two specs it is scoped
by: `docs/superpowers/specs/2026-09-20-interactive-demo-templates-design.md` (§3.1
`useLocalCollection` contract, §3.2 row #6 the save-listing feature, §3.3 `SavedItemsPanel`, §5
per-site Hallmark requirement, §7 the build+interaction test gate, §8 the 10-criterion design
scoring gate, §9 execution order) and `docs/superpowers/specs/2026-09-20-template-design-system-design.md`
(§4 component behavior/prop contracts, including the Real Estate worked example, §6
folder/thumbnail/slug convention).

## Global Constraints

- **Hard prerequisite: plan 30 must be committed first.** Do not start Task 2 until
  `@portfolio/template-kit`'s barrel export (`template-kit/src/index.ts`) exists and its own test
  suite passes — this plan imports from it, it does not vendor or reimplement any kit component,
  including `SavedItemsPanel`.
- **`slug` = `realestate`, `subdomain` = `realestate`, `display_order` = 6** — fixed by plan 30 Task
  1's authoritative slug table; do not deviate.
- No comments restating what code does; only comments explaining non-obvious "why" (same rule as
  plan 30/31).
- **No fabricated content.** Seed data (the 8 listings, and the empty saved-listings seed) must
  match `docs/superpowers/specs/2026-09-20-site-realestate-design.md` §3/§4 exactly — no invented
  price/size statistics presented as real, no Lorem Ipsum above the fold.
- **Static content must never be gated behind `useLocalCollection`'s hydration** (interactive-demo
  spec §3.1's render-pattern rule) — `LISTINGS` renders directly from imported seed data via
  `ItemGrid` in the server-rendered/static-exported page; only the per-card save-state (derived from
  the hook's `items`), the `SaveToggleOverlay`, and the `SavedItemsPanel` live inside a client
  component that mounts after the static shell is visible.
- **`ItemGrid` (kit component) stays untouched.** The per-listing "Save" toggle is a separate
  client-only overlay (spec §4.3 point 2), not a prop added to `ItemGrid` and not a fork of it —
  matches master spec §5's "shared behavior, bespoke visual design" rule.
- Every composed page must render at 320/375/414/768px without horizontal scroll (same rule
  `template-kit` components are already built to, per plan 30's Global Constraints — this plan is
  responsible for not breaking it at the page-composition level, including the save-toggle overlay
  staying aligned with `ItemGrid`'s cards at every breakpoint, not just desktop).
- `next.config.js` must set `output: 'export'` (parent spec §6, unchanged).
- `public/thumbnail.webp` is required and validated by `template-kit/scripts/check-thumbnail.mjs`
  (plan 30 Task 2) wired into this site's own `package.json` `build` script — this plan does not
  reimplement the checker, only calls it.
- Do not fabricate Hallmark token values in this plan document. Task 1 states the process and the
  required output shape; the real hex/font/spacing values only exist after the real session runs.
- Do not fabricate Task 3's design-score numbers in this plan document. The score table is written
  with criterion names and an empty/TBD score column — real scores are filled in by whoever executes
  Task 3, after implementation, per master spec §8.
- Current baseline: re-read `docs/superpowers/STATUS.md`'s own header for the actual current
  pass count before reporting — plans may have landed between this plan's authoring and its
  execution.

---

## Task 1: Hallmark design pass

**Files:**
- Create: `templates/realestate/theme.ts`
- Create: `templates/realestate/app/globals.css`
- Create (Hallmark's own working artifacts, per the skill's normal output — moodboard/reference
  notes, wherever the `hallmark` skill places them): no fixed path prescribed here; follow the
  skill's own convention.

**Interfaces:**
- Consumes: `TemplateTheme` type and `assertValidTheme` validator from
  `@portfolio/template-kit` (`template-kit/src/theme.ts`, shipped by plan 30 Task 2) — this site's
  `theme.ts` must conform to that shape and call `assertValidTheme` on its own exported value so a
  malformed token set fails the build loudly instead of shipping `undefined` CSS variables.
- Produces: `templates/realestate/theme.ts` (the validated `TemplateTheme` value this site commits
  to) and `templates/realestate/app/globals.css` (the `--color-*`/`--font-*`/`--space-*` custom
  properties `template-kit`'s components read at render time, populated with this site's real
  values instead of placeholders).

### Design decision

**(a) This is a real Hallmark session, not a scaffold placeholder.** Per interactive-demo spec §5,
every one of the 29 sites gets its own full `hallmark` skill pass (greenfield path), same rigor as
the main portfolio site's own pass and as plan 31's corporate site — not a value picked from the
parent spec's old 4-cluster enum (that enum is explicitly overridden, per plan 30 decision (f)).
The input to the session is this plan's own spec,
`docs/superpowers/specs/2026-09-20-site-realestate-design.md` §6 ("Hallmark design brief") —
industry mood (real-estate brokerage, trust-driven and approachable), the non-binding soft-cool/
humanist-sans starting direction, and the 3 reference directions to research. The session may land
anywhere its research supports, including deviating from that starting direction, as long as the
result clears every criterion in master spec §8 at Task 3 time (particularly #1 anti-generic, #2
typographic craft, #3 color coherence, #10 distinctiveness against the other 28 sites — especially
`corporate`, `medical`, and `travel`, which share a similar cool/neutral starting family).

- [ ] **Step 1: Invoke the `hallmark` skill, greenfield path**, supplying it this plan's context:
  the site is a real-estate brokerage demo landing page (`Hero` → listings `ItemGrid` (with a
  static map-placeholder graphic per card) → `InquiryForm` → `Footer`, plus a save-listing overlay
  and a saved-listings panel section), industry mood per spec §6, and the constraint that output
  must conform to `template-kit`'s `TemplateTheme` shape (`accentHue: string`, `displayFont:
  string`, `bodyFont: string` — plus whatever additional `--space-*`/motion tokens the session
  decides are needed for `globals.css`, which are not constrained by `TemplateTheme`'s type since
  that type only governs the 3 fields `template-kit`'s components read directly).
- [ ] **Step 2: Run the session's research phase** — the skill's own process for the 2-3 reference
  directions named in the spec (Vietnamese real-estate brokerage/listing-portal design, boutique
  international real-estate/property marketing, wayfinding/static-cartography design systems for
  the map-placeholder treatment), converging on one direction with a rationale, not a blend of all
  three.
- [ ] **Step 3: Design the static map-placeholder graphic treatment** as part of the same session
  (spec §4.4/§6 reference direction 3) — since this site has no live map embed, the session must
  decide how the `mapPlaceholderImage` graphic reads as intentional design (a stylized static
  cartography style, tied to the site's token palette) rather than as a missing/broken map. Record
  the decision (illustration approach, asset source/generation method) in the session's own notes.
- [ ] **Step 4: Produce `templates/realestate/theme.ts`** exporting a `TemplateTheme`-conformant
  object and calling `assertValidTheme` on it at module scope (so an invalid theme throws at import
  time, not silently):

```typescript
import { assertValidTheme, type TemplateTheme } from '@portfolio/template-kit';

const theme: TemplateTheme = {
  accentHue: /* real value from the Hallmark session, not a placeholder */ '',
  displayFont: /* real value from the Hallmark session */ '',
  bodyFont: /* real value from the Hallmark session */ '',
};

export default assertValidTheme(theme);
```

  (The empty-string right-hand sides above are illustrative of the shape only — the real file
  committed in this step must have the session's actual values; an empty string would fail
  `assertValidTheme` itself and is not an acceptable committed state.)

- [ ] **Step 5: Produce `templates/realestate/app/globals.css`** defining the `--color-*`/
  `--font-*`/`--space-*` (and any motion-duration/easing tokens the session's interaction-polish
  direction needs, per master spec §8 criterion #5 — including a distinct hover/active state for the
  per-card Save toggle) custom properties `template-kit`'s components and this site's own
  `SaveToggleOverlay` read, with real values matching `theme.ts`'s exported `TemplateTheme` — no
  ad-hoc hex/OKLCH values outside this token file (master spec §8 criterion #3 requires every color
  trace to a token).
- [ ] **Step 6: Verify WCAG AA contrast** on every real text/background token pairing the session
  defines (criterion #3), including the Save toggle's saved/unsaved states, which must both be
  distinguishable by more than color alone (criterion #7 accessibility) — using the Hallmark skill's
  own audit capability or an equivalent contrast checker; record the pass in the session's own
  notes.
- [ ] **Step 7: Verify `theme.ts` imports and `assertValidTheme` succeeds** —
  `cd templates/realestate && npx tsx theme.ts` (or equivalent quick run) exits 0, no thrown error.
- [ ] **Step 8: Commit** — `design: Hallmark pass for realestate site theme`

**Acceptance criteria for this task:** `templates/realestate/theme.ts` exists, type-checks against
`TemplateTheme`, and its module-scope `assertValidTheme` call does not throw; `globals.css` defines
real (non-placeholder) values for every token `template-kit` components and the save-toggle overlay
consume; the direction (including the map-placeholder graphic treatment) is traceable to the spec's
Hallmark brief (§6) and its research phase, not asserted without process.

---

## Task 2: Next.js app scaffold + static page composition

**Files:**
- Create: `templates/realestate/package.json`, `templates/realestate/tsconfig.json`,
  `templates/realestate/next.config.js`
- Create: `templates/realestate/app/layout.tsx`, `templates/realestate/app/page.tsx`
- Create: `templates/realestate/data/seed.ts`
- Create: `templates/realestate/public/thumbnail.webp` (placeholder image — see step 6 note)
- Create: `templates/realestate/public/listings/map-placeholder-0{1..8}.webp` (placeholder graphics
  — see step 7 note)
- Test: `templates/realestate/scripts/build-gate.test.mjs`

**Interfaces:**
- Consumes: `Hero`, `ItemGrid`, `InquiryForm`, `Footer` from `@portfolio/template-kit`'s barrel
  export (plan 30 Task 5); `theme.ts`/`globals.css` from Task 1;
  `template-kit/scripts/check-thumbnail.mjs` (plan 30 Task 2) wired into this `package.json`'s
  `build` script.
- Produces: the static page at `templates/realestate/app/page.tsx` that this plan's own Task 3
  extends with the save-listing interactive section; the `npm run build` gate every later Task in
  this plan (and any future maintenance) relies on.

### Design decisions

**(b) `data/seed.ts` holds `LISTINGS` and the `toGridItem`/`formatListingSummary` mapping
functions** exactly as shaped in
`docs/superpowers/specs/2026-09-20-site-realestate-design.md` §3.1/§3.2 — `page.tsx` imports
`LISTINGS` directly and maps it to `ItemGrid`'s `GridItem[]` at render time, never behind a hook,
per the Global Constraints render-pattern rule.

**(c) `public/thumbnail.webp` and the 8 `public/listings/map-placeholder-0N.webp` files in this
step are real, valid WEBP files (placeholder imagery is acceptable, invalid/missing files are
not)** — the build-gate check (plan 30 Task 2's `checkThumbnail`) asserts real `RIFF`/`WEBP` magic
bytes for the thumbnail; the map-placeholder images are not build-gated by `checkThumbnail` (that
script only checks `public/thumbnail.webp`) but must still be real image files so `ItemGrid`'s
`<img>` rendering isn't silently broken in the static export. Use placeholder WEBP exports (e.g.
neutral generated graphics matching this site's Task 1 accent color and map-placeholder treatment)
until an admin/Task-1 asset is finalized, per parent spec §6's convention — this step's job is to
satisfy the build gate and avoid broken `<img>` tags honestly, not to fake either check.

**(d) `formatListingSummary` omits the bed/bath segment for `propertyType: 'land'` listings**
(spec §3.1's note on `listing-7`) — a formatting branch in `data/seed.ts`, not a special-cased
`GridItem`.

- [ ] **Step 1: Write the failing build-gate test** — asserts `npm run build` (which runs
  `check-thumbnail.mjs` then `next build`) exits 0 and `out/` (or the configured static-export
  output dir) contains an `index.html`:

```javascript
// templates/realestate/scripts/build-gate.test.mjs
import { describe, it, expect } from 'vitest';
import { execSync } from 'node:child_process';
import { existsSync } from 'node:fs';
import path from 'node:path';

const ROOT = path.resolve(import.meta.dirname, '..');

describe('realestate site build gate', () => {
  it('npm run build succeeds and produces a static export with index.html', () => {
    execSync('npm run build', { cwd: ROOT, stdio: 'pipe' });
    expect(existsSync(path.join(ROOT, 'out', 'index.html'))).toBe(true);
  });

  it('public/thumbnail.webp exists and is a valid WEBP before build runs', () => {
    const { checkThumbnail } = require('../../template-kit/scripts/check-thumbnail.mjs');
    const result = checkThumbnail(path.join(ROOT, 'public', 'thumbnail.webp'));
    expect(result.ok).toBe(true);
  });

  it('all 8 listing map-placeholder images exist', () => {
    for (let i = 1; i <= 8; i++) {
      const file = path.join(ROOT, 'public', 'listings', `map-placeholder-0${i}.webp`);
      expect(existsSync(file)).toBe(true);
    }
  });
});
```

- [ ] **Step 2: Run to verify it fails** — `cd templates/realestate && npx vitest run
  scripts/build-gate.test.mjs` (package/app don't exist yet, expect failure).
- [ ] **Step 3: `templates/realestate/package.json`**

```json
{
  "name": "realestate-template",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "next dev",
    "build": "node ../../template-kit/scripts/check-thumbnail.mjs public/thumbnail.webp && next build",
    "test": "vitest run"
  },
  "dependencies": {
    "@portfolio/template-kit": "workspace:*",
    "next": "15.5.4",
    "react": "19.2.0",
    "react-dom": "19.2.0"
  },
  "devDependencies": {
    "@types/node": "^24",
    "@types/react": "19.2.2",
    "typescript": "5.6.3",
    "vitest": "5.0.1"
  }
}
```

- [ ] **Step 4: `templates/realestate/next.config.js`**

```javascript
/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
};

module.exports = nextConfig;
```

- [ ] **Step 5: `templates/realestate/tsconfig.json`** — same shape as
  `template-kit/tsconfig.json` (plan 30 Task 2 Step 4) with `jsx: "preserve"` and Next.js's standard
  `plugins`/`paths` additions per the installed Next.js version's own scaffold defaults.
- [ ] **Step 6: Add `public/thumbnail.webp`** — a placeholder WEBP image (any tool capable of
  emitting a real `RIFF`/`WEBP` file; content is not load-bearing for this step, validity is).
- [ ] **Step 7: Add the 8 `public/listings/map-placeholder-0N.webp` files** — placeholder WEBP
  images (real `RIFF`/`WEBP` files; content matches Task 1 Step 3's map-placeholder treatment once
  Task 1 lands, generic neutral graphics are acceptable as an interim commit if Task 2 is executed
  before Task 1's asset is finalized, as long as they are real, valid images, not broken links).
- [ ] **Step 8: `templates/realestate/data/seed.ts`** — the `PropertyListing`/`LISTINGS` array and
  the `toGridItem`/`formatListingSummary` functions exactly as specified in
  `docs/superpowers/specs/2026-09-20-site-realestate-design.md` §3.1/§3.2 (copy verbatim from the
  spec, including all 8 listings — do not trim or invent additional rows).
- [ ] **Step 9: `templates/realestate/app/layout.tsx`**

```tsx
import type { Metadata } from 'next';
import './globals.css';

export const metadata: Metadata = {
  title: 'Sông Hồng Real Estate',
  description: 'Curated apartment, townhouse, villa, and land listings across Vietnam.',
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="vi">
      <body>{children}</body>
    </html>
  );
}
```

- [ ] **Step 10: `templates/realestate/app/page.tsx`** — imports `Hero`, `ItemGrid`, `InquiryForm`,
  `Footer` from `@portfolio/template-kit`, and `LISTINGS`/`toGridItem` from `../data/seed`; composes
  them in the exact section order from spec §2 (`Hero` → [save-listing section placeholder wired in
  Task 3] → `ItemGrid` (fed `LISTINGS.map(toGridItem)`) → `InquiryForm` (base fields +
  `INQUIRY_EXTRA_FIELDS` from spec §5) → `Footer`). Task 3 adds the client-component save-listing
  section between `Hero` and `ItemGrid`, plus the `SaveToggleOverlay` positioned against `ItemGrid`
  — this step may leave that slot as an explicit, clearly named empty `<section>` comment marker
  (not a TODO left unresolved at plan end — Task 3 fills it in the same plan) so the page composes
  and builds correctly before Task 3 lands.
- [ ] **Step 11: Run `cd templates/realestate && npm install && npm run build`, then `npx vitest
  run scripts/build-gate.test.mjs`, verify pass.**
- [ ] **Step 12: Mutation checks**

| # | Revert | Test that must go RED |
|---|---|---|
| M1 | remove `output: 'export'` from `next.config.js` | the static-export `out/index.html` existence assertion |
| M2 | replace `public/thumbnail.webp` with a non-WEBP file of the same name | the thumbnail-valid-WEBP test |
| M3 | drop `check-thumbnail.mjs` from the `build` npm script | the build-gate test (build would succeed on an invalid thumbnail, silently passing when it should fail first) |
| M4 | delete one of the 8 `map-placeholder-0N.webp` files | the all-8-map-placeholders-exist test |

- [ ] **Step 13: Commit** — `feat: scaffold realestate template Next.js app with static section composition`

---

## Task 3: Save-listing interactive feature + design scoring

**Files:**
- Create: `templates/realestate/app/SaveListingsSection.tsx` (client component: hook owner,
  `SavedItemsPanel`, and `SaveToggleOverlay`)
- Modify: `templates/realestate/app/page.tsx` (wire `SaveListingsSection` into the slot left by
  Task 2 Step 10)
- Test: `templates/realestate/app/SaveListingsSection.test.tsx`

**Interfaces:**
- Consumes: `useLocalCollection`, `SavedItemsPanel` from `@portfolio/template-kit`; `LISTINGS` from
  `../data/seed`.
- Produces: the site's one required interaction test per master spec §7 ("exactly one interaction
  test exercising its `useLocalCollection` feature end-to-end" — save → appears in panel → reload
  persists → unsave → gone); the final design-score table that marks this plan complete per master
  spec §8.

### Design decisions

**(e) `SaveListingsSection` is a client component (`'use client'`)** mounted inside the otherwise
static-exported `page.tsx`, per the Global Constraints render-pattern rule — it owns the single
`useLocalCollection` call for the whole page, renders `SavedItemsPanel`, and renders
`SaveToggleOverlay` (per-listing Save/Saved buttons, positioned against the separately-rendered,
untouched `ItemGrid`, per spec §4.3 point 2). The rest of the page (`Hero`, `ItemGrid`,
`InquiryForm`, `Footer`) stays server/static-rendered.

**(f) `SaveToggleOverlay` derives each button's saved/unsaved state by checking `items.some(i =>
i.id === listing.id)`** — there is no separate "is this saved" boolean tracked anywhere else; the
hook's `items` array is the single source of truth, so the overlay and the panel can never disagree
about a listing's saved state.

- [ ] **Step 1: Write the failing interaction test**

```tsx
// templates/realestate/app/SaveListingsSection.test.tsx
import { describe, it, expect, beforeEach } from 'vitest';
import { render, screen, fireEvent, waitFor } from '@testing-library/react';
import { SaveListingsSection } from './SaveListingsSection';

beforeEach(() => {
  window.localStorage.clear();
});

describe('SaveListingsSection', () => {
  it('shows the empty state before any listing is saved', () => {
    render(<SaveListingsSection />);
    expect(screen.getByText(/no saved listings yet/i)).toBeInTheDocument();
  });

  it('saving a listing adds it to the saved-listings panel', async () => {
    render(<SaveListingsSection />);
    fireEvent.click(screen.getByRole('button', { name: /save 12 đường nguyễn huệ/i }));

    await waitFor(() => {
      expect(screen.getByText(/12 đường nguyễn huệ/i)).toBeInTheDocument();
    });
  });

  it('persists a saved listing across remount (simulated reload)', async () => {
    const { unmount } = render(<SaveListingsSection />);
    fireEvent.click(screen.getByRole('button', { name: /save 45 đường trần não/i }));
    await waitFor(() => expect(screen.getByText(/45 đường trần não/i)).toBeInTheDocument());
    unmount();

    render(<SaveListingsSection />);
    await waitFor(() => expect(screen.getByText(/45 đường trần não/i)).toBeInTheDocument());
  });

  it('unsaving a listing removes it from the panel and flips the toggle back', async () => {
    render(<SaveListingsSection />);
    fireEvent.click(screen.getByRole('button', { name: /save 12 đường nguyễn huệ/i }));
    await waitFor(() => expect(screen.getByText(/12 đường nguyễn huệ/i)).toBeInTheDocument());

    fireEvent.click(screen.getByRole('button', { name: /saved 12 đường nguyễn huệ/i }));
    await waitFor(() => {
      expect(screen.queryByText(/12 đường nguyễn huệ/i)).not.toBeInTheDocument();
      expect(screen.getByText(/no saved listings yet/i)).toBeInTheDocument();
    });
  });

  it('unsaving from the panel itself also flips the card toggle back', async () => {
    render(<SaveListingsSection />);
    fireEvent.click(screen.getByRole('button', { name: /save 12 đường nguyễn huệ/i }));
    await waitFor(() => expect(screen.getByText(/12 đường nguyễn huệ/i)).toBeInTheDocument());

    fireEvent.click(screen.getByRole('button', { name: /remove/i }));
    await waitFor(() => {
      expect(screen.getByText(/no saved listings yet/i)).toBeInTheDocument();
      expect(screen.getByRole('button', { name: /save 12 đường nguyễn huệ/i })).toBeInTheDocument();
    });
  });
});
```

- [ ] **Step 2: Run to verify it fails** — `cd templates/realestate && npx vitest run
  app/SaveListingsSection.test.tsx` (module doesn't exist yet).
- [ ] **Step 3: `templates/realestate/app/SaveListingsSection.tsx`**

```tsx
'use client';
import { useLocalCollection, SavedItemsPanel } from '@portfolio/template-kit';
import { LISTINGS } from '../data/seed';

interface SavedListing {
  id: string;
  title: string;
  savedAt: string;
}

const SAVED_SEED: SavedListing[] = [];

export function SaveListingsSection() {
  const { items, add, remove, reset } = useLocalCollection<SavedListing>(
    'realestate-saved',
    SAVED_SEED,
  );

  function isSaved(listingId: string) {
    return items.some((saved) => saved.id === listingId);
  }

  function toggle(listingId: string, address: string) {
    if (isSaved(listingId)) {
      remove(listingId);
    } else {
      add({ id: listingId, title: address, savedAt: new Date().toISOString() });
    }
  }

  return (
    <section className="realestate-save-listings">
      <div className="realestate-save-toggle-overlay" aria-label="Save a listing">
        {LISTINGS.map((listing) => {
          const saved = isSaved(listing.id);
          return (
            <button
              key={listing.id}
              type="button"
              aria-pressed={saved}
              onClick={() => toggle(listing.id, listing.address)}
            >
              {saved ? `Saved ${listing.address}` : `Save ${listing.address}`}
            </button>
          );
        })}
      </div>
      <aside aria-label="Saved listings">
        <h3>Saved listings</h3>
        <SavedItemsPanel
          items={items}
          emptyLabel="No saved listings yet — tap Save on a listing to add it here"
          onRemove={remove}
          renderItem={(saved: SavedListing) => saved.title}
        />
        <button type="button" onClick={reset}>Reset demo data</button>
      </aside>
    </section>
  );
}
```

- [ ] **Step 4: Wire `SaveListingsSection` into `page.tsx`** — replace Task 2 Step 10's marker with
  `<SaveListingsSection />`, positioned between `Hero` and `ItemGrid` per spec §2; ensure the
  Hallmark pass's CSS (Task 1) visually aligns `.realestate-save-toggle-overlay`'s buttons with the
  separately-rendered `ItemGrid` cards below/around it (implementation detail owned by Task 1's
  token output + this task's markup, not a new kit component).
- [ ] **Step 5: Run `cd templates/realestate && npx vitest run`, verify all pass.**
- [ ] **Step 6: Run the full build gate again** — `npm run build`, confirm still green after wiring
  `SaveListingsSection` in.
- [ ] **Step 7: Mutation checks**

| # | Revert | Test that must go RED |
|---|---|---|
| M5 | make `toggle()` always call `add()`, never `remove()` (save-only, no true toggle) | the unsaving-removes-from-panel test |
| M6 | seed `SAVED_SEED` with a non-empty fake saved listing instead of `[]` | the empty-state-before-any-save test |
| M7 | drop the `useEffect`-driven read-from-`localStorage` path (i.e. break `useLocalCollection` usage by seeding from `useState(() => [])` locally instead of calling the hook) | the persists-across-remount test |
| M8 | wire `SavedItemsPanel`'s `onRemove` to a no-op instead of `remove` | the unsave-from-panel-flips-card-toggle test |

- [ ] **Step 8: Commit** — `feat: add save-listing interactive feature to realestate site`

### Design score

Per master spec §8: every criterion must reach 9-10/10 before this plan is marked complete; a
criterion that cannot without a real trade-off must say so with a one-line reason instead of an
inflated number. This table is filled in by whoever executes this task, after the real
implementation and a real Hallmark `audit` pass exist to score against — the scores below are
intentionally left blank at plan-authoring time (master spec §8: scoring happens after
implementation, not during planning).

| # | Criterion | Score (0-10) | Notes |
|---|---|---|---|
| 1 | Anti-generic / anti-AI-slop | TBD | |
| 2 | Typographic craft | TBD | |
| 3 | Color system coherence | TBD | |
| 4 | Layout/spacing rhythm | TBD | |
| 5 | Motion & interaction polish | TBD | |
| 6 | Responsive integrity | TBD | |
| 7 | Accessibility | TBD | |
| 8 | Content authenticity | TBD | |
| 9 | Interaction correctness | TBD | |
| 10 | Brand/industry distinctiveness | TBD | |

- [ ] **Step 9: Run the Hallmark `audit` capability** against the built site, score each criterion
  honestly (criterion #7 accessibility must specifically verify the Save toggle is keyboard-operable
  and its saved/unsaved state is conveyed via `aria-pressed`, not color alone; criterion #6
  responsive integrity must verify the save-toggle overlay stays aligned with `ItemGrid`'s cards at
  320/375/414/768px), fill in the table above, and fix/re-score any criterion below 9/10 before
  considering this plan complete.
- [ ] **Step 10: Commit** — `docs: plan 36 complete — realestate site design score`

## Self-Review Notes

- **Spec coverage:** `2026-09-20-site-realestate-design.md` §2 (Task 2's section composition), §3
  (Task 2's seed data and the `GridItem` mapping), §4 (Task 3's save-listing feature and the
  `SavedItemsPanel`-fits-here rationale, contrasted with corporate's plain-list choice), §5 (Task
  2's inquiry-form extra field), §6 (Task 1's Hallmark brief). Interactive-demo spec §3.1 (Task 3's
  hook usage), §3.2 row #6 (the feature itself), §3.3 (`SavedItemsPanel`'s intended use case), §5
  (Task 1's per-site Hallmark requirement), §7 (Task 2/3's build + interaction test gates), §8 (Task
  3's design-score table), §9 (this plan is "plan 36" in that section's numbering). Parent spec §4
  (Task 2's component contracts, including the Real Estate worked example naming the map-embed
  placement), §6 (Task 2's folder/thumbnail/`output: 'export'` convention).
- **Explicitly not built here:** any of the other 28 site plans (31-35, 37-59); changes to
  `@portfolio/template-kit` itself (any new component or hook change, including any future
  per-item-action prop on `ItemGrid`, belongs in a plan-30 follow-up, not here); CI wiring for this
  site's static-export deploy (plan 17, parameterized for 29 sites, unchanged by this plan); real
  thumbnail/map-placeholder asset upload through the admin media library (admin action, not a code
  task, beyond the placeholder assets this plan's Task 2 commits to satisfy the build gate).
- **Dependency confirmed:** this plan assumes plan 30's barrel export
  (`template-kit/src/index.ts`) already ships `Hero`, `ItemGrid`, `InquiryForm`, `Footer`,
  `SavedItemsPanel`, `useLocalCollection`, `TemplateTheme`, `assertValidTheme` — if plan 30 is not
  yet complete when this plan is executed, Task 2 cannot start; verify plan 30's own commit history
  first.
- **Next step:** once this plan's design score (Task 3) clears every criterion at 9-10/10 (or
  documents an honest trade-off), the remaining site plans in §9's sequence may reuse this plan's
  structure — in particular its `SaveToggleOverlay`-alongside-`ItemGrid` pattern — as precedent for
  Agency, Travel, and Photography, the other three categories in master spec §3.3's
  `SavedItemsPanel` row.
